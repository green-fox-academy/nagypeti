package com.dbintegration.connecmysql;

import com.dbintegration.connecmysql.controller.TodoController;
import com.dbintegration.connecmysql.repository.Todo;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ConnecmysqlApplication implements CommandLineRunner {

  public static void main(String[] args) {
    SpringApplication.run(ConnecmysqlApplication.class, args);
  }

  @Override
  public void run(String... args) {
//    TodoController.todoService.addTodo(new Todo("Create task1 basics"));
//    TodoController.todoService.addTodo(new Todo("Create task1 with JPA DI"));
//    TodoController.todoService.addTodo(new Todo("Connect MySQL database with app"));
//    TodoController.todoService.addTodo(new Todo("Patch the ozone hole"));
//    TodoController.todoService.addTodo(new Todo("Do funny things"));
//    TodoController.todoService.addTodo(new Todo("Eat chinese food"));
//    TodoController.todoService.addTodo(new Todo("Watch movie with teargas"));
//    TodoController.todoService.addTodo(new Todo("Ride a bear"));
//    TodoController.todoService.addTodo(new Todo("Drink too much and vomit"));
  }

}

